// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/deicod/archivedgames/ent/game"
	"github.com/deicod/archivedgames/ent/gamelike"
)

// GameLike is the model entity for the GameLike schema.
type GameLike struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID string `json:"user_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GameLikeQuery when eager-loading is set.
	Edges          GameLikeEdges `json:"edges"`
	game_like_game *string
	selectValues   sql.SelectValues
}

// GameLikeEdges holds the relations/edges for other nodes in the graph.
type GameLikeEdges struct {
	// Game holds the value of the game edge.
	Game *Game `json:"game,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// GameOrErr returns the Game value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e GameLikeEdges) GameOrErr() (*Game, error) {
	if e.Game != nil {
		return e.Game, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: game.Label}
	}
	return nil, &NotLoadedError{edge: "game"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GameLike) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case gamelike.FieldID, gamelike.FieldUserID:
			values[i] = new(sql.NullString)
		case gamelike.ForeignKeys[0]: // game_like_game
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GameLike fields.
func (_m *GameLike) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case gamelike.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case gamelike.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.String
			}
		case gamelike.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field game_like_game", values[i])
			} else if value.Valid {
				_m.game_like_game = new(string)
				*_m.game_like_game = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GameLike.
// This includes values selected through modifiers, order, etc.
func (_m *GameLike) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryGame queries the "game" edge of the GameLike entity.
func (_m *GameLike) QueryGame() *GameQuery {
	return NewGameLikeClient(_m.config).QueryGame(_m)
}

// Update returns a builder for updating this GameLike.
// Note that you need to call GameLike.Unwrap() before calling this method if this GameLike
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *GameLike) Update() *GameLikeUpdateOne {
	return NewGameLikeClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the GameLike entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *GameLike) Unwrap() *GameLike {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: GameLike is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *GameLike) String() string {
	var builder strings.Builder
	builder.WriteString("GameLike(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(_m.UserID)
	builder.WriteByte(')')
	return builder.String()
}

// GameLikes is a parsable slice of GameLike.
type GameLikes []*GameLike
